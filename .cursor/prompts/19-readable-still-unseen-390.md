# Round 19 — the mark lands; I still cannot read the sentence

Reviewed 2026-08-23 08:38 UTC on `:4173` (Round 18 preview, 07:27). Loaded
at **390×844**, searched Kyoto, opened the hit, tapped **Mark as visited**
(no Show), undid it, and ran

```
node test/acceptance.mjs http://127.0.0.1:4173/
```

Last line: **`102/102 passed`**. Exit 0.

**How to talk back:** write in `.cursor/AGENT-REPLY.md`. Do not edit this file.

This round is **twelve minutes**. Do not start a backend.

---

## What I clicked — Round 18 is closed

Mark control **358×44**. One tap, no dialog. Label went **Mark as visited**
→ **Marked as visited** (accent on the mark only). Same tap undid it.
Camera stayed `[12, 24]` zoom **2.1** — Africa, not Honshu. The DOM
sentence did change:

- before: lake & river, rural vernacular & agrarian, and modern
  metropolis & industry were still in the list
- after: those three dropped
- undo: the long sentence came back

Your suite quoted a shorter after-sentence because it marks after the
1440 Show fly and leftover Afghanistan marks. The traveler path above is
the one I will keep using.

---

## This round — the bar still reads the same

The 128px overlay ellipsizes at **"high m..."** before the mark and
after the mark. The kinds that dropped live in the tail. A traveler
looking at the map cannot see that anything changed. The sentence is
the product.

**Done means:**

- Viewport **390×844**. Search Kyoto. Open. Mark from the sheet.
- Without opening the register, I can **read a change** on
  `.filters.on-map`: either the visible Still unseen line (no
  ellipsis hiding the drop) or a coverage count on that bar that
  moves, e.g. kinds seen. Bar stays **≤128**. Camera stays.
- A check that fails if, at 390 after that mark, the visible text of
  `.filters.on-map` is unchanged to `checkVisibility()` text / the
  un-clipped range. Do not pass on `innerText` that overflows. Quote
  doc 3 §8 and §12.

I will mark Kyoto and screenshot the bar. If it still says
"high m..." on both sides, it is not done.

---

## Parked

Accounts, sync, `/api`, street/satellite/photoreal. Bevel. The 35 ids.
Kyoto → Ishikawa. `punchHoles`. Do not build them.

---

## Questions

Answer in `.cursor/AGENT-REPLY.md`:

1. Quote the last line of acceptance after a preview started *after*
   this build.
2. The visible (not overflowed) bar text before and after the mark.
3. Did you put the coverage count on the bar, or reflow the sentence?

Standing rules: `.cursor/rules/travelers-world-map.mdc` outranks this file,
and `docs/2` outranks both.
