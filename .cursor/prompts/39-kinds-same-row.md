# Round 39 — overlap CSS did not move Kinds onto All’s row

Reviewed 2026-08-23 14:12 UTC on `:4173` (overlap-CSS preview, 14:05).
AGENT-REPLY empty. Idle 390, same numbers as Round 38:

- All row top **85**, Kinds top **131**, **delta 46**, `sameLine:
  false`, `overlap: false`
- Bar **116**
- Layers **60×44**
- Hide the register **44×44**

The segs are a horizontal cluster. **Kinds of place is still the row
under them.** Overlap CSS did not land.

**How to talk back:** write in `.cursor/AGENT-REPLY.md` *before* you
ship. Two empty replies on this item.

This round is **twelve minutes**. Do not start accounts or Express.

---

## This round — put Kinds on All’s line

`grid-row` / a second block under the segs is the miss. Put
`.filter-more` on the **same top** as `.filter-row` (±8px). Labels may
ellipsize; the 44×44 hit may not. Bar **≤128**. Layers stays.

**Done means:**

- Viewport **390×844**.
  `Math.abs(filter-row.top - filter-more.top) < 8`
- Bar **≤128**. Every tap on that bar ≥44×44. Layers ≥44×44.
- A check that fails if delta ≥8. Do **not** pass on height ≤128
  alone. Quote the owner and doc 3 §12.

Place-count (distributed cap on the 15,770) is next.

I will measure the two tops.

---

## Parked

Accounts, sync, sign-up. Satellite / photoreal / Google panoramas.
Bevel. The 35 ids. Kyoto → Ishikawa. `punchHoles`. Wrapping nine kinds
on the 390 sentence. Do not build them.

---

## Questions

Answer in `.cursor/AGENT-REPLY.md`:

1. Quote the last line of acceptance after a preview started *after*
   this build.
2. All top, Kinds top, delta, bar height.
3. Did you write this reply before the preview?

Standing rules: `.cursor/rules/travelers-world-map.mdc` outranks this file,
and `docs/2` outranks both.
