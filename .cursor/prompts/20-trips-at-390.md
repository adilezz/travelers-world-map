# Round 20 — I can read 0 of 9 → 3 of 9; trips at 390 is next

Reviewed 2026-08-23 08:54 UTC on `:4173` (Round 19 preview, 07:43). Loaded
at **390×844**, searched Kyoto, marked from the sheet, and ran

```
node test/acceptance.mjs http://127.0.0.1:4173/
```

Last line: **`103/103 passed`**. Exit 0.

**How to talk back:** write in `.cursor/AGENT-REPLY.md`. Do not edit this file.

This round is **twelve minutes**. Do not start a backend.

---

## What I clicked — Round 19 is closed

The compact count is on the bar, unclipped, **79×14**, class
`coverage-compact-count`. Before the mark: **`0 of 9 kinds`**. After:
**`3 of 9 kinds`**. The screenshot reads **0 OF 9 KINDS** then **3 OF 9
KINDS**. Still unseen still ellipsizes; you said you would not reflow it.
Bar stayed **128**. Camera stayed `[12, 24]` zoom **2.1**. Coverage
count, not a country percentage. Your reply matches.

---

## This round — add the stop to a trip at 390

Desktop trips work: Day 1, a quiet line, no duration. At 390 I have not
added a stop. The sheet I opened has Mark and Show. Add to a trip has to
be reachable on that sheet, at least **44×44**, and must not move the
camera.

**Done means:**

- Viewport **390×844**. Search Kyoto. Open the hit. **Add to** a trip
  from the sheet (scroll the sheet if needed; do not `force`-click a
  hidden node). The stop lands on **Day 1**. No time or duration. No
  confirmation.
- Camera stays. The filter bar stays **≤128** (or tucks smaller). The
  trips surface must not bury the globe.
- A check that fails if, at 390, Add to is missing, smaller than 44px,
  moves the camera, or leaves the stop in Unassigned. Quote doc 2 §9
  and doc 3 §12.

I will search Kyoto, look for Add to, tap it, and read Day 1.

---

## Parked

Accounts, sync, `/api`, street/satellite/photoreal. Bevel. The 35 ids.
Kyoto → Ishikawa. `punchHoles`. Do not build them.

---

## Questions

Answer in `.cursor/AGENT-REPLY.md`:

1. Quote the last line of acceptance after a preview started *after*
   this build.
2. Add to: width × height and `checkVisibility()`.
3. After the tap: Day 1 names, camera, bar height.

Standing rules: `.cursor/rules/travelers-world-map.mdc` outranks this file,
and `docs/2` outranks both.
