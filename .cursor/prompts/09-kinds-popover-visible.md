# Round 9 — Tiles and 66/66 are real; the kinds popover is not visible

Reviewed 2026-08-23 05:45 UTC on `:4173` (Round 8 preview, 04:32). Clicked
Hide the register → Tiles, opened Kinds of place, and ran

```
node test/acceptance.mjs http://127.0.0.1:4173/
```

Last line: **`66/66 passed`**. Exit 0. That matches your reply.

**How to talk back:** write in `.cursor/AGENT-REPLY.md`. Do not edit this file.

This round is **twelve minutes**. Do not start a backend.

---

## What I clicked

- **Tiles:** `#view-tiles` is on, pitch **42**, style has
  `fill-extrusion:tile-extrude`. Card stays **340×280** with Still unseen.
  Atlas is still there to return. No `punchHoles` in what the map is
  running. The Tiles check PASSed. Done.
- **Resting height:** opening the Kinds of place `<summary>` left the card
  at **280 → 280**. Popover claim holds for the box.
- Dark-theme check PASSed in the suite. I did not finish a headed dark
  screenshot this tick (the kinds click threw first).

---

## What is still wrong — this is the round

I opened **Kinds of place** on the full-screen card. The summary exists.
**Zero chips had a visible box** (`height > 4`). Playwright then found
`[data-kind="A5"]` Desert & steppe in the DOM and refused to click it:
**Element is not visible**.

So the suite's *tapping a kind on the full-screen card filters both
surfaces* is clicking a node a traveler cannot see. That is how 66/66
and a broken popover coexist.

**Done means:**

- After Hide the register, open Kinds of place. The twelve kinds are
  **visible** — `checkVisibility()` / a non-zero painted box, not merely
  in the document. 44×44. Shape and label, no colour language, no accent
  on chrome.
- Tap desert & steppe. Register count changes. Same tap releases.
  Card stays 340×280 at rest.
- Change the kinds-on-card check so it **fails if the row is not
  visible**. Do not `force: true`. Do not weaken the 280 check.

I will open the summary and screenshot the chips. If I cannot see them,
the round is not done.

---

## Parked

Accounts, sync, `/api`, street/satellite/photoreal. Bevel. The 35 ids.
Kyoto → Ishikawa. `punchHoles`. Do not build them.

---

## Questions

Answer in `.cursor/AGENT-REPLY.md`:

1. Quote the last line of acceptance after a preview started *after*
   this build.
2. Where is the kinds popover positioned (`position: fixed` to what)?
   Why did getBoundingClientRect height come back 0 for the chips?
3. Confirm the kinds-on-card check now fails if the row is hidden.

Standing rules: `.cursor/rules/travelers-world-map.mdc` outranks this file,
and `docs/2` outranks both.
