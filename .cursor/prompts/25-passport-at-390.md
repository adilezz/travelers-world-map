# Round 25 — Import restored the mark; passport on the 390 bar is next

Reviewed 2026-08-23 10:34 UTC on `:4173` (Round 24 preview, 09:23). At
**390×844** I marked Kyoto, exported, cleared storage, imported from the
header label, and the agent's suite on that preview ended

```
112/112 passed
```

**How to talk back:** write in `.cursor/AGENT-REPLY.md`. You skipped it
this round. Do not skip it again.

This round is **twelve minutes**. Do not start a backend.

---

## What I clicked — Round 24 is closed

Import **57×44**, `vis: true`. After mark: **3 of 9 kinds**. After a
clean load: **0 of 9 kinds**. After Import of that file: **3 of 9
kinds** again. Kyoto `aria-pressed=true`. Camera stayed `[12, 24]` zoom
**2.1**. No password. Screenshot: **3 OF 9 KINDS**, **Marked as visited**.
Suite: `countAfterImport: 3 of 9 kinds`, `wall: false`, `camMoved: false`.

---

## This round — passport on the 390 bar

Desktop passport annotates the register and says it is not legal advice.
On the 390 bar, **PASSPORT** has been an empty label. Entry requirements
are part of looking at a place. They must not use the accent.

**Done means:**

- Viewport **390×844**. A passport control on `.filters.on-map` is
  visible without `force`, at least **44×44**.
- Choosing a passport annotates the surfaces and states it is not legal
  advice. The bar stays **≤128**. No accent on that chrome.
- A check that fails if, at 390, that control is missing or choosing it
  does nothing. Quote doc 3 §12.

I will open the bar at 390 and look for passport.

---

## Parked

Accounts, sync, `/api`, street/satellite/photoreal. Bevel. The 35 ids.
Kyoto → Ishikawa. `punchHoles`. Do not build them.

---

## Questions

Answer in `.cursor/AGENT-REPLY.md`:

1. Quote the last line of acceptance after a preview started *after*
   this build.
2. The passport control's width × height and `checkVisibility()`.
3. After choosing one, the exact not-legal-advice sentence I should read.

Standing rules: `.cursor/rules/travelers-world-map.mdc` outranks this file,
and `docs/2` outranks both.
