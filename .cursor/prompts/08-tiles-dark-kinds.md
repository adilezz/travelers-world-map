# Round 8 — the box is 280; click Tiles, dark, and kinds on the card

Reviewed 2026-08-23 05:28 UTC on `:4173` (Round 7 preview, 04:16).
Clicked, measured, ran `node test/acceptance.mjs http://127.0.0.1:4173/`.

Last line: **`59/59 passed`**. Exit 0.

**How to talk back:** write in `.cursor/AGENT-REPLY.md`. Do not edit this file.

This round is **twelve minutes**. Do not start a backend.

---

## What I clicked — Round 7 is closed

Your reply was right, and the numbers now match the node I measure:

| Path | `.filters.on-map` | Still unseen |
|---|---|---|
| 1440, Hide the register | **340×280** | yes |
| 1440 → 390 (your path) | **374×128** | yes |
| 390 cold load | **374×128** | yes |

Register sheet at 390 is 390×354 under a 390×434 map. Globe is not under
a 300px card. The host/inner mismatch is gone. Thank you for writing the
reply.

Owner items from this thread (close, full-screen map, filters on the map,
Still unseen on the card, 390) are in a state I can defend.

---

## This round — three clicks I have not taken on this chrome

I have not, on the **full-screen card**, done Tiles, dark theme, or the
kinds disclosure. That is twelve minutes.

### 1. Tiles, with the register hidden

Atlas / Tiles sit top-right, off the card. Click Tiles. The globe must
become the territory tiles. Filters, Still unseen, and Show the register
stay. Selecting still never zooms. Marking still never moves the camera.
Holes are still a pipeline job — do not call `punchHoles`.

**Done means:** I can Hide the register, click Tiles, see extruded tiles,
and get back to Atlas. Add a check that quotes doc 3 §6.1 (no camera
from the view switch if that is already true) or doc 2's atlas/tiles
toggle — only if one does not already exist. Do not weaken 340×280.

### 2. Dark theme on that same layout

The header moon. Accent still means visited and nothing else (the suite
already walks light). I will screenshot dark + full-screen. No new
colour on the map.

### 3. Kinds of place on the card actually filter

The twelve-row meter was replaced by a disclosure so the card fits. Open
it. Tap a kind that has places (desert & steppe). The map and the
register (when shown again) must narrow. Same control releases. 44×44.
No accent on the chrome. The coverage sentence stays.

If the disclosure blows the card past 280px, it may be a popover — the
card's resting box stays 340×280.

**Done means:** a check that collapsing the column, opening kinds, and
tapping a kind filters, quoting doc 3 §8. I will tap it.

---

## Parked

Accounts, sync, `/api`, street/satellite/photoreal. Bevel. The 35 ids.
Kyoto → Ishikawa. `punchHoles`. Do not build them.

---

## Questions

Answer in `.cursor/AGENT-REPLY.md`:

1. Quote the last line of acceptance after a preview started *after*
   this build.
2. With the register hidden, does Tiles keep the 340×280 card?
3. Does the kinds disclosure change the card's resting height, or is it
   a popover?

Standing rules: `.cursor/rules/travelers-world-map.mdc` outranks this file,
and `docs/2` outranks both.
