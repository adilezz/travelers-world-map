# Round 24 — Export is on the header; Import has to take the file back

Reviewed 2026-08-23 10:08 UTC on `:4173` (Round 23 preview, 08:56). Loaded
at **390×844**, read the header, tapped Export, and ran

```
node test/acceptance.mjs http://127.0.0.1:4173/
```

Last line: **`111/111 passed`**. Exit 0.

**How to talk back:** write in `.cursor/AGENT-REPLY.md`. Do not edit this file.

This round is **twelve minutes**. Do not start a backend.

---

## What I clicked — Round 23 is closed

Header at 390: **THE WORLD**, **Trips**, **Export**, **Import**, theme.
Export **57×44**, `vis: true`, not clipped, not the accent. Theme
**44×44**. Brand name is gone (12px) so those fit. Tapping Export wrote
`travelers-world-map-2026-08-23.json`. No password. No sign-up. Suite:
Import also **57×44**. Day 1 pick and Unassigned-move checks are in
(107 → 111). Your reply matches.

---

## This round — Import at 390, no account

Export is how the record leaves. Import is how it comes back. This app
has no server. I have not tapped Import on the phone header.

**Done means:**

- Viewport **390×844**. Import is still **≥44×44**, visible, not clipped.
- Mark Kyoto from the sheet. Export. Load a clean session. Import that
  file from the header (the `<label class="link-btn file">`, not a
  hidden input `force`-click). No sign-up wall.
- After import, the bar reads **3 of 9 kinds** (or the same count as
  after the mark) and Kyoto is marked. Camera was not asked to move.
- A check that fails if, at 390, that import does not restore the mark,
  or if a password field appears. Quote doc 2 §10.

I will mark, export, reload, import, and read the bar.

---

## Parked

Accounts, sync, `/api`, street/satellite/photoreal. Bevel. The 35 ids.
Kyoto → Ishikawa. `punchHoles`. Do not build them.

---

## Questions

Answer in `.cursor/AGENT-REPLY.md`:

1. Quote the last line of acceptance after a preview started *after*
   this build.
2. After import, the visible coverage count on `.filters.on-map`.
3. Did a password or sign-up field appear?

Standing rules: `.cursor/rules/travelers-world-map.mdc` outranks this file,
and `docs/2` outranks both.
