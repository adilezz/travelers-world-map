# Round 15 — Day 1 takes the stops; I need to see the line

Reviewed 2026-08-23 07:25 UTC on `:4173` (Round 14 preview, 06:12). Hid
the register, opened Trips, added Kyoto then Tadrart Acacus from
`.search-hit`, and ran

```
node test/acceptance.mjs http://127.0.0.1:4173/
```

Last line: **`89/89 passed`**. Exit 0.

**How to talk back:** write in `.cursor/AGENT-REPLY.md`. Do not edit this file.

This round is **twelve minutes**. Do not start a backend.

---

## What I clicked — Round 14 is closed

Both places landed under **DAY 1**, not the tray. UNASSIGNED reads
*Drop a place here.* Camera stayed `[2.1, 12, 24]`. Day select **102×44**.
Card **340×280**. The hop is labelled **11224 km · a long hop** — a
distance, not a duration. No times.

Add-to lands on Day 1. Unassigned is a tray. Your reply matches what I
tapped.

---

## This round — the segment has to be on the globe

The suite reports LineString of 4 via the test hook. I asked MapLibre's
style sources after the two adds and got **`types: []`, `lineCoords: 0`**.
The screenshot of that state is the globe (Africa / Europe) with no
stroke I can call a trip.

A traveler who hid the register to plan on the map is looking at the
map. The line is the trip.

**Done means:**

- Hide the register. Two Day 1 stops. A **straight segment** is painted
  on the globe (not a route). Quiet stroke — not the accent.
- I can find it without `_twmTrip`: a map layer or source whose
  LineString coordinates match the two places, or a canvas stroke I can
  see in the screenshot.
- Adding still does not move the camera. 340×280 holds.
- A check that fails if, after two assigned stops with the register
  hidden, the trip source/layer has no LineString. Do not only assert
  JSON in a test hook. Quote doc 2 §9.

I will add Kyoto and Acacus again and look for the stroke.

---

## Parked

Accounts, sync, `/api`, street/satellite/photoreal. Bevel. The 35 ids.
Kyoto → Ishikawa. `punchHoles`. Do not build them.

---

## Questions

Answer in `.cursor/AGENT-REPLY.md`:

1. Quote the last line of acceptance after a preview started *after*
   this build.
2. What is the MapLibre source/layer id for the trip line?
3. At zoom 2.1, is the Kyoto–Acacus segment expected to cross the
   disc I screenshot, or is it behind the globe?

Standing rules: `.cursor/rules/travelers-world-map.mdc` outranks this file,
and `docs/2` outranks both.
