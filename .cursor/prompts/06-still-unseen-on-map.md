# Round 6 — 48/48 holds; Still unseen vanished off the full-screen map

Reviewed 2026-08-23 04:56 UTC on the preview started 03:49 UTC
(`npx vite preview --port 4173`). Clicked, screenshotted, and ran

```
node test/acceptance.mjs http://127.0.0.1:4173/
```

Last line: **`48/48 passed`**. Exit 0. That matches your reply. Good.

**How to talk back:** write in `.cursor/AGENT-REPLY.md`. Do not edit this file.

This round is **twelve minutes**. Do not start a backend.

---

## What I clicked — your Round 5 numbers are real

- **`map.loaded() === true`** PASS. The globe draws. Rome's country link
  no longer crashes the suite.
- **✕, empty-map, Escape** all dismiss Kyoto. Empty-map:
  `{ open: false, hidden: true }`. Camera unmoved.
- **Passport is first in the column** (y≈143), above the coverage meter
  (y≈494). No longer below the fold.
- **Hide the register:** map **1440×844**, filter card **~340×280** at
  top-left, Atlas/Tiles at top-right, kinds of place a disclosure
  (`kindsVisible: 0` until opened), chevron reads **Show the register**.
  The globe is the interface again. That is what the owner asked for.

Voice rules still hold. Orphans, trips, bulk still pass. Parked items
stayed parked.

---

## What is still wrong — this is the round

### 1. Full-screen has no "Still unseen"

That sentence is the product. After Hide the register, the card has All /
Not visited / Visited, search, passport, World Heritage, and a "Kinds of
place" disclosure. The coverage meter — `0 of 9 kinds of place seen` and
**Still unseen: …** — is gone with the column.

A traveler who puts the map full screen is looking at the product with
its voice switched off.

**Done means:**

- In the full-screen card, the coverage sentence is present. One or two
  lines is enough: the count and "Still unseen: …". Not the twelve-row
  meter; that would bring the slab back. The kind disclosure can stay
  the way into the list.
- Tapping a kind from that disclosure still filters, still does not use
  the accent, still 44×44.
- The card stays compact. If the sentence would push the overlay past
  ~280px, wrap or clip with a way to read it — do not grow a form over
  the globe again. I measured ~92% of the map uncovered; stay in that
  band.
- Add a check: *collapsing the column does not hide the coverage
  sentence* (doc 2's product sentence / doc 3 §8). Quote the rule. Do
  not weaken the 340×280 overlay check.

### 2. Click it at 390×844

I did not take a mobile pass this tick. You must, because Hide the
register and the on-map card were built at 1440.

**Done means:** on a 390px-wide window, a traveler can filter, mark,
dismiss with ✕ / empty map / Escape, and still reach the register
(peek/half/full sheet). Tap targets 44×44. The globe is not buried
under the card. If the desktop card is too wide, it is not a desktop
card copied onto a phone.

I will screenshot 390 and 1440 next tick.

---

## Parked

Accounts, sync, `/api`, street/satellite/photoreal. Bevel. The 35 ids.
Kyoto's tile (`JAP-c1857910` → Ishikawa) — pipeline. Do not build them.

---

## Questions

Answer in `.cursor/AGENT-REPLY.md`:

1. Quote the last line of acceptance after a preview started *after*
   this build.
2. On 1440, after Hide the register, does the card show the words
   "Still unseen"? I will look for them.
3. What happens to the on-map card at 390px? One sentence.

Standing rules: `.cursor/rules/travelers-world-map.mdc` outranks this file,
and `docs/2` outranks both.
