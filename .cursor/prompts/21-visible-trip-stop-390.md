# Round 21 — Kyoto is on Day 1; I cannot see the stop on the card

Reviewed 2026-08-23 09:11 UTC on `:4173` (Round 20 preview, 07:59). Loaded
at **390×844**, searched Kyoto, tapped **Add to a trip**, and ran

```
node test/acceptance.mjs http://127.0.0.1:4173/
```

Last line: **`106/106 passed`**. Exit 0.

**How to talk back:** write in `.cursor/AGENT-REPLY.md`. Do not edit this file.

This round is **twelve minutes**. Do not start a backend.

---

## What I clicked — Round 20 is closed

**Add to a trip** is **358×44**, `vis: true`, under Show, not the accent,
no dialog. Camera stayed `[12, 24]` zoom **2.1**. Bar tucked to **48**.
Trips card **374×156** on a **434px** map, `buried: false`. DOM Day 1 is
**Kyoto**; Unassigned is *Drop a place here.* No times. On a cold 390
that is the first stop, as you said.

---

## This round — the card has to show the stop

The screenshot of that state is a **New trip** button and the words
"New trip" — not Kyoto, not DAY 1. The suite passes on DOM names inside
a 156px card that is mostly chrome. A traveler adding a stop is looking
at the card.

**Done means:**

- Viewport **390×844**. Search Kyoto. Add to a trip.
- **Kyoto** on Day 1 is `checkVisibility()` true without `force`, at
  least **44×44**, inside the on-map trips card, not clipped under the
  New trip button.
- Bar still **≤128** (48 is fine). Card still must not bury the globe
  (≤45% of map height). Camera stays. No duration.
- A check that fails if that stop's visible box is missing or shorter
  than 44px. Quote doc 2 §9 and doc 3 §12.

I will add Kyoto and screenshot the trips card. If I still only see
New trip, it is not done.

---

## Parked

Accounts, sync, `/api`, street/satellite/photoreal. Bevel. The 35 ids.
Kyoto → Ishikawa. `punchHoles`. Do not build them.

---

## Questions

Answer in `.cursor/AGENT-REPLY.md`:

1. Quote the last line of acceptance after a preview started *after*
   this build.
2. The stop's width × height and `checkVisibility()`.
3. After Add to, the trips card height and what I should see first.

Standing rules: `.cursor/rules/travelers-world-map.mdc` outranks this file,
and `docs/2` outranks both.
