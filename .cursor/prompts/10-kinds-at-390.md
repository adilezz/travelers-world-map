# Round 10 — kinds are visible on 1440; do the same at 390

Reviewed 2026-08-23 06:00 UTC on `:4173` (Round 9 preview, 04:48). Clicked
Hide the register → Kinds of place → Desert & steppe, and ran

```
node test/acceptance.mjs http://127.0.0.1:4173/
```

Last line: **`68/68 passed`**. Exit 0.

**How to talk back:** write in `.cursor/AGENT-REPLY.md`. Do not edit this file.

This round is **twelve minutes**. Do not start a backend.

---

## What I clicked — Round 9 is closed

Twelve kind chips, all `vis: true`, all **44px** tall. Desert & steppe is
**112×44** at (138, 448). Clicking it narrowed the register

`15,770 places worldwide · 0 marked` → `4,996 of 15,770 places worldwide`

Card stayed **340×280**. Your clip/containing-block story was right. The
check without `force: true` is why 68/68 now means something.

---

## This round — 390px

The thin bar is **374×128**. I have not opened Kinds of place there.

**Done means:**

- Cold 390 load, and 1440→390 after Hide the register.
- Open Kinds of place. Chips are **visible** (`checkVisibility()`, box
  ≥ 44px). I can tap desert & steppe without `force`. Same tap releases.
- The popover must not bury the globe the way the old 301px card did.
  Resting bar stays ≤ 128px. Popover can be absolute in `.map-wrap`; it
  must not cover the whole 434px map.
- Still unseen stays on the bar. Sheet still is the register.
- A check at 390 that fails if the chip is hidden. Quote doc 3 §8 / §11.
  Do not weaken 374×128 or 340×280.

I will screenshot 390 with the chips open.

Dark theme on 1440 full-screen is still only suite-green from here. If
you have minutes left, click the moon once so I can screenshot it. Not
instead of 390.

---

## Parked

Accounts, sync, `/api`, street/satellite/photoreal. Bevel. The 35 ids.
Kyoto → Ishikawa. `punchHoles`. Do not build them.

---

## Questions

Answer in `.cursor/AGENT-REPLY.md`:

1. Quote the last line of acceptance after a preview started *after*
   this build.
2. At 390, is the kinds popover the same `.map-wrap` absolute panel or a
   different layout?
3. Roughly how tall is the open popover on 390? I will measure it.

Standing rules: `.cursor/rules/travelers-world-map.mdc` outranks this file,
and `docs/2` outranks both.
