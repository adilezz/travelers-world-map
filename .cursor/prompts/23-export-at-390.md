# Round 23 — the Day 1 pick works; Export is off the screen at 390

Reviewed 2026-08-23 09:51 UTC on `:4173` (Round 21 preview, still
serving). Loaded at **390×844**, searched Kyoto, added it, used the Day
1 select, and ran

```
node test/acceptance.mjs http://127.0.0.1:4173/
```

Last line: **`107/107 passed`**. Exit 0. Your reply answered Round 21's
questions, not this round's. The pick was already in the Round 21 build.

**How to talk back:** write in `.cursor/AGENT-REPLY.md`. Answer *these*
questions.

This round is **twelve minutes**. Do not start a backend.

---

## What I clicked — Round 22 is closed

`.trip-day-pick` next to Kyoto: **102×44**, `vis: true`, in the card,
value `1`. ✕ is **44×44**. Choosing Unassigned moved Kyoto to the tray
(DAY 1 reads *Drop a place here.*). Camera stayed `[12, 24]` zoom
**2.1**. No times. Screenshot after: **UNASSIGNED / Kyoto / Unassigned**.

Add a check for that select and the Unassigned move if one is missing
(the suite is still 107). Do not restyle it.

---

## This round — Export at 390

Every 390 screenshot clips the header after **Trips**. THE WORLD and
Trips fit. **Export**, **Import**, and the theme toggle do not. Export
is how a mark leaves this browser. A traveler on a phone cannot reach
it.

**Done means:**

- Viewport **390×844**. **Export**, **Import**, and the theme control
  are `checkVisibility()` true without `force`, at least **44×44**, not
  clipped by the header.
- Export still writes the record (orphans stay in the file). No
  account wall. Accent stays off chrome.
- A check that fails if, at 390, Export is missing, clipped, or smaller
  than 44px. Quote doc 3 §11 and doc 2 §10.

I will load 390 and tap Export.

---

## Parked

Accounts, sync, `/api`, street/satellite/photoreal. Bevel. The 35 ids.
Kyoto → Ishikawa. `punchHoles`. Do not build them.

---

## Questions

Answer in `.cursor/AGENT-REPLY.md`:

1. Quote the last line of acceptance after a preview started *after*
   this build.
2. Export at 390: width × height and `checkVisibility()`.
3. Did tapping Export produce a file without a sign-up wall?

Standing rules: `.cursor/rules/travelers-world-map.mdc` outranks this file,
and `docs/2` outranks both.
