# Round 18 — Kyoto is on the disc at 390; marking from that sheet is next

Reviewed 2026-08-23 08:20 UTC on `:4173` (preview restarted 07:12). Loaded
at **390×844**, searched Kyoto on the bar, opened the hit, tapped **Show
Kyoto on the map**, and ran

```
node test/acceptance.mjs http://127.0.0.1:4173/
```

Last line: **`96/96 passed`**. Exit 0.

**How to talk back:** write in `.cursor/AGENT-REPLY.md`. Do not edit this file.

This round is **twelve minutes**. Do not start a backend.

---

## What I clicked — Round 17 is closed

Idle bar **374×128** with Still unseen. Search hit **354×44**, `vis: true`,
bar **122**. Selecting left the camera at `[12, 24]` zoom **2.1**. **Show
Kyoto on the map** is **358×44**, `vis: true`, not the accent, on
`.detail:not([hidden])` (the sheet, not `.detail-on-map`). The tap flew
to **center `[135.75, 34.92]`, zoom `4.71`**. The after screenshot is
Honshu with Kyoto ringed. Atlas stayed Atlas. Bar returned to **128**
and Still unseen came back after the search closed.

Your reply matches. Ishikawa stays parked.

---

## This round — Mark as visited from that sheet

The sheet I opened has **Mark as visited** sitting above Show on the map.
That is the action. The 390 suite marks from a register row. It does not
mark from the place sheet after a card search.

**Done means:**

- Viewport **390×844**. Search Kyoto on the bar. Open the hit. Tap
  **Mark as visited** on the sheet. One tap, no confirmation. The same
  tap undoes it.
- The camera does not move. The on-map bar's **Still unseen** sentence
  changes when the mark lands (and changes back on undo). The mark
  control is at least **44×44**. The accent may fill the mark; it must
  not appear on other chrome.
- A check that fails if, at 390, marking from that sheet moves the
  camera, opens a dialog, or leaves the bar's Still unseen unchanged.
  Quote doc 2 §7 and doc 3 §8.

I will search Kyoto, mark from the sheet, and read the bar.

---

## Parked

Accounts, sync, `/api`, street/satellite/photoreal. Bevel. The 35 ids.
Kyoto → Ishikawa. `punchHoles`. Do not build them.

---

## Questions

Answer in `.cursor/AGENT-REPLY.md`:

1. Quote the last line of acceptance after a preview started *after*
   this build.
2. After the mark, the exact Still unseen text on `.filters.on-map`.
3. Did the camera move? Quote center and zoom before and after.

Standing rules: `.cursor/rules/travelers-world-map.mdc` outranks this file,
and `docs/2` outranks both.
