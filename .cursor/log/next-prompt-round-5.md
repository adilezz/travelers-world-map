# Round 5 — close works; the suite dies mid-run; the map is not full screen

Reviewed 2026-08-23 04:38 UTC by clicking `:4173` (preview started 03:35)
and running `node test/acceptance.mjs http://127.0.0.1:4173/`.

**How to talk back:** write in `.cursor/AGENT-REPLY.md`. Do not edit this file.

This round is **twelve minutes**. Do not start a backend.

---

## What you fixed — I clicked it

I read the Round 4 reply. Then I drove the app.

- **✕ closes the city.** Kyoto → ✕ → `.detail[hidden]`. The new check
  *the close control dismisses the sheet without moving the camera* **PASS**.
  Escape still works. That was the owner's first item. Done.
- **Selecting opens the detail panel** PASS. Passport's three checks PASS.
  Voice rules still hold. Orphans, trips, bulk still pass when the suite
  gets that far.
- **Hide the register** exists. After it, the map element is 1440px wide
  and `.filters.on-map` is in the DOM. You added the three owner checks
  for that. Good — I will click them once the suite can reach them.
- Kyoto's tile is pipeline. `JAP-c1857910` / `JPN-T07` / Ishikawa. Parked.

Your 43/43 claim is still not what I get on the preview I hit. See below.

---

## What is still broken

### 1. The suite does not finish. This is the round.

My run on the 03:35 preview:

```
FAIL  the map style loaded (map.loaded() === true)
…then twenty-odd PASSes, including close and passport…
then a crash:

  elementHandle.click: Element is not attached to the DOM
  test/acceptance.mjs:287
  await countryLink.click()   // .detail .link-btn.v after searching Rome
```

The globe **is** on screen. `map.loaded()` is still false when the suite
asks. That is the same flap as last round. Do not weaken
`map.loaded() === true`. Fix boot so the check is boring.

The crash is a re-render: you keep a handle to the country link and click
it after the sheet has been replaced. **Do not delete the check.** Make the
sheet stable enough that a country link survives until the click, or wait
for a live node the way a traveler would. The suite must exit 0.

**Done means:** a preview you start *after* this build, then

```
node test/acceptance.mjs http://127.0.0.1:4173/
```

prints a pass count and exit 0. Quote that count in the reply. If it is
not 43/43 or whatever the file now contains, say so — do not round up.

### 2. Full-screen map still is not the map.

The owner asked for a **full-screen map with filters in it**. What I
clicked after Hide the register:

- The column is gone (panel width 0). That part is right.
- Filters, kinds, months, passport and Clear filters then occupy a **large
  slab above the globe**. The globe is no longer the interface; it is a
  remainder under a form. Atlas/Tiles sit in that slab, not on the globe.

That is the 400px column stood on its head. Same failure: chrome ate the
map.

**Done means:**

- After Hide the register, **most of the pixels are the globe**. Doc 3's
  60% floor at 1440px still applies when the column is gone — filters are
  chrome on the map, not a second page.
- A thin bar on the map is enough: Atlas/Tiles, search, visited/not,
  passport, World Heritage. Kinds of place can be one compact row or a
  disclosure. They are distinguished by shape and label, never colour.
- No accent on that chrome.
- Show the register must bring the column back. Keyboard still reaches
  every place.
- Selecting never zooms. Marking never moves the camera.

The checks you added at the bottom of `acceptance.mjs` (column collapse
does not hide filtering; filter chrome is not the accent; Show the
register) stay. The suite has to *reach* them.

### 3. Empty map click should dismiss too

✕ and Escape dismiss. I could not prove empty-canvas click does, because
✕ had already closed the sheet. Same three-way contract as last round:
✕, empty map, Escape. No camera move.

### 4. Passport is still below the fold when the column is open

You wrote that `#passport-pick` now sits under search. On this preview,
with the register open, it is still at y≈840 in a 900px window. When the
column is showing, a traveler must be able to choose a passport without
opening full-screen. Move it, for real, or the next click-through will
say the same sentence again.

---

## Parked

Accounts, sync, street/satellite/photoreal. Bevel. The 35 ids and Kyoto's
tile (pipeline). Do not build any of them.

---

## Questions

Answer in `.cursor/AGENT-REPLY.md`:

1. Quote the exact acceptance command and the last line it printed
   after a preview you started *after* the build. Not 03:02, not 03:35.
2. After Hide the register, roughly what fraction of the window is the
   globe? I will screenshot it.
3. Confirm empty-map click dismisses a selected city.

Standing rules: `.cursor/rules/travelers-world-map.mdc` outranks this file,
and `docs/2` outranks both.
