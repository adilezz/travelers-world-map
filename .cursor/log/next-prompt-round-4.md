# Round 4 — map is up; close is broken; put filters on a full-screen map

Reviewed 2026-08-23 04:20 UTC, then updated 04:24 UTC after the owner
spoke and after a second click-through on `127.0.0.1:5173`.

**How to talk back:** write in `.cursor/AGENT-REPLY.md`. I read it at the start
of every round and answer here. Do not edit this file — I overwrite it each
round and your edits would be lost.

This round is **twelve minutes**. Owner bugs first. Do not start a backend.

---

## What you fixed (I can see it in the browser now)

- **The globe draws.** `map.loaded() === true` on both :5173 and :4173.
  Atlas shows land, borders, and cluster counts.
- **Trips, bulk, orphans** pass in the suite.
- **The voice is still right.** No accent on chrome, no "archetype", no
  completion percentage, score reads in its country frame. Coverage speaks
  in "Still unseen".
- Onboarding has no sign-up wall. That stays.

Your claim of **43/43** does not match the run I made (38/43). See § suite.

---

## Owner, just now — these are this round

Two things from the owner, both confirmed by clicking. They outrank the
passport tidy-up if you run out of time.

### 1. The ✕ does not deselect the city. Fix this first.

Reproduced on Kyoto:

- Search Kyoto → row click → `.detail` opens (`Close detail` / ✕).
- Click ✕. **The sheet stays.** Kyoto is still on the panel.
  `detailHidden` remains false. The register row loses `aria-selected`,
  so the list thinks nothing is selected while the city sheet is still up.
- **Escape does dismiss it** (`.detail[hidden]` becomes true). So the
  dismiss path exists; the close control is not wired to it.
- Clicking empty map after Escape does not bring the sheet back. Fine.

**Done means:** the ✕ (`aria-label="Close detail"`), clicking empty map,
and Escape all do the same thing: hide the sheet, clear selection, no
camera move. Same tap target 44×44. Add an acceptance check that quotes
this: *selecting never zooms; dismissing must also not move the camera,
and the close control must actually dismiss* (doc 3 §6.1). Do not weaken
the existing Escape check.

### 2. Full-screen map, with the filters on the map.

Today the map is 1034×844 under a 56px header, with a **400px panel** on
the right. Atlas / Tiles already sit on the map. Everything a traveler
filters with lives in that panel, and half of it is below the fold:

- All / Not visited / Visited, Search places — in the panel
- World Heritage — in the panel
- **PASSPORT** and `#passport-pick` — in the panel at y≈839 in a 900px
  window, so I never saw them. That is why Round 4 thought passport was
  missing. It is there; it is buried.
- Kind chips and the coverage sentence — in the panel

The owner wants the **map full screen** and those **filters on the map**,
not trapped in the column.

**Done means:**

- A traveler can put the map edge-to-edge (header may stay; the 400px
  column must be able to go away). The existing panel chevron is the
  start of this, not the finish — collapsing today hides the filters
  with the column.
- In that full-screen state, the filters that change what is on the map
  sit **on the map**: kinds of place, visited / not visited, World
  Heritage, search, passport. Atlas / Tiles stay where they are.
- 44×44 tap targets. Cool neutrals. **No accent on filter chrome** — the
  accent is visited and nothing else.
- The register does not disappear as a product. It is the accessible
  equivalent of the map (doc 3 §11). Full-screen is a layout; the
  register must remain reachable (peek sheet, or a control that brings
  the column back) and every place still keyboard-reachable.
- Selecting still never zooms. Marking still never moves the camera.
  Filters still remove; scope still emphasises.
- Do not invent a second colour language for kinds. Shape and label only.

If twelve minutes is not enough for a polished overlay, land the
**layout**: full-screen map + the existing filter bar moved onto the map
chrome, register still openable. Pretty is a later round.

Add checks, each quoting the rule:

- collapsing the column does not hide filtering (owner, this round)
- filter chrome does not use the accent (doc 3 §3)
- the register remains reachable when the map is full screen (doc 3 §11)

---

## Stack (unchanged — still do not build these)

| Surface | Present? |
|---|---|
| Web app | Yes |
| Place database (15,770) | Yes |
| Accounts / auth / user DB | No |
| Application backend / sync | No |
| Street / satellite / photoreal | No |

Parked. Doc 2 §10 forbids a sign-up wall.

---

## The suite I ran — 38/43 — still required

`node test/acceptance.mjs http://127.0.0.1:4173/` at 03:17 UTC:

```
FAIL  the map style loaded (map.loaded() === true)
FAIL  selecting opens the detail panel
FAIL  choosing a passport annotates the register
FAIL  the passport layer says it is not legal advice
FAIL  the passport filter narrows both surfaces   no chip
```

Passport is in the panel, below the fold. Once filters sit on the map,
those three should become clickable. Do not change the checks.

Restart preview after you build. Claim 43/43 only against a preview you
just started.

**Selecting opens the detail panel** is `page.click('.row')` then
`.detail:not([hidden])`. Search-Kyoto opened a sheet for me; the suite's
first `.row` path must too.

---

## Parked — do not decide these yourself

**Accounts, sync, street view.** Still no.

**The bevel.** Still parked.

**The 35 lost place ids.** Database decision.

**Kyoto's tile.** Detail still says **Ishikawa Prefecture**. Say in the
reply whether that string is on the bundle.

---

## Questions I have for you

Answer in `.cursor/AGENT-REPLY.md`:

1. Against which URL did you see 43/43? Quote the command.
2. Confirm ✕, empty-map click, and Escape all dismiss the city. I will
   click them.
3. Confirm you will not add accounts, a sync API, or street/satellite
   view this round.

Standing rules: `.cursor/rules/travelers-world-map.mdc` outranks this file,
and `docs/2` outranks both. The owner's two items in this file are
explicit product direction for this round.
