# Round 38 — Kinds is still 46px below All

Reviewed 2026-08-23 13:58 UTC on `:4173` (preview 13:53). AGENT-REPLY
empty. Idle 390:

- All row top **85**, Kinds top **131**, **delta 46**, `sameLine:
  false`
- Bar **116**
- Layers **60×44**
- No tap under 44×44 on that bar

Round 37 did not land. The segs are horizontal; **Kinds of place is a
second row**. A height≤128 check is not this.

**How to talk back:** write in `.cursor/AGENT-REPLY.md`.

This round is **twelve minutes**. Do not start accounts or Express.

---

## This round — same Done as 37

Owner: the filter should be horizontal. Still unseen may sit above.
All, Not visited, Visited, Search, passport, **Kinds of place** sit on
**one line** (`Math.abs(topA - topB) < 8`). Every tap **≥44×44**. Bar
at 390 **≤128**. Layers stays 44×44 in the corner.

**Done means:**

- Viewport **390×844**. `.filters.on-map > .filter-row` and
  `.filter-more` share one row. Bar **≤128**. Taps ≥44×44.
- A check that fails if that delta is ≥8. Do **not** weaken it to
  bar height ≤128. Quote the owner and doc 3 §12.
- Layers menu unchanged (Geographical / Street / Tiles).

Place-count is **next** after this row exists.

I will measure All vs Kinds tops again.

---

## Parked

Accounts, sync, sign-up. Satellite / photoreal / Google panoramas.
Bevel. The 35 ids. Kyoto → Ishikawa. `punchHoles`. Wrapping nine kinds
on the 390 sentence. Do not build them.

---

## Questions

Answer in `.cursor/AGENT-REPLY.md`:

1. Quote the last line of acceptance after a preview started *after*
   this build.
2. All row top, Kinds top, delta, bar height.
3. Layers width × height.

Standing rules: `.cursor/rules/travelers-world-map.mdc` outranks this file,
and `docs/2` outranks both.
