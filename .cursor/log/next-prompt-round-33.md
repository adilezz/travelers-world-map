# Round 33 — Hide works; The world is 28px tall

Reviewed 2026-08-23 12:04 UTC on `:4173` (Round 32 preview, 11:52).
Hide, map height, Show, then my re-run:

```
121/121 passed
```

I clicked `.panel-collapse` without `force`. **Hide the register**
**44×44**, not the accent. Map **434 → 788**. Bar **116**, Still unseen
stays. **Show the register** **44×44** brings `.panel` and `.row` back.
Round 32 is closed. AGENT-REPLY was empty again — write the numbers
next time.

**How to talk back:** write in `.cursor/AGENT-REPLY.md`.

This round is **twelve minutes**. Do not start a backend.

---

## This round — The world is a tap, so it is 44×44

On the 390 header, **The world** is **108×28**. After Hide, that
control is how you leave a country. Doc 3 §11 is width and height.
28px is not a tap.

**Done means:**

- Viewport **390×844**. The world in the header is **≥44×44**,
  `checkVisibility()` true, clickable without `force`, not the accent.
- Hide / Show the register stays ≥44×44. After Hide, map height
  **≥700**. Bar **≤128**. Bar taps stay ≥44×44.
- A check that fails if The world’s width or height is under 44. Do
  not weaken the hide-register checks. Quote doc 3 §11 and §12.

I will measure The world, then Hide the register.

---

## Parked

Accounts, sync, `/api`, street/satellite/photoreal. Bevel. The 35 ids.
Kyoto → Ishikawa. `punchHoles`. Wrapping the idle Still unseen list at
390. Do not build them.

---

## Questions

Answer in `.cursor/AGENT-REPLY.md`:

1. Quote the last line of acceptance after a preview started *after*
   this build.
2. The world: width × height, `checkVisibility()`. Hide-register size
   and map height after Hide.
3. Bar height at rest.

Standing rules: `.cursor/rules/travelers-world-map.mdc` outranks this file,
and `docs/2` outranks both.
