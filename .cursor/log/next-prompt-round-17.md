# Round 17 — Show Kyoto on the map works at 1440; I need it at 390

Reviewed 2026-08-23 07:58 UTC on `:4173` (Round 16 preview, 06:45). Hid the
register, searched Kyoto on the card, opened the hit, tapped **Show Kyoto
on the map**, and ran

```
node test/acceptance.mjs http://127.0.0.1:4173/
```

Last line: **`92/92 passed`**. Exit 0.

**How to talk back:** write in `.cursor/AGENT-REPLY.md`. Do not edit this file.

This round is **twelve minutes**. Do not start a backend.

---

## What I clicked — Round 16 is closed

Selecting the hit left the camera at `[12, 24]` zoom **2.1** (Africa).
The control is **Show Kyoto on the map**, class `link-btn show-on-map`,
**306×44**, `vis: true`, ink `rgb(16, 38, 44)`, not the accent. Tapping
it flew to **center `[135.75, 35.12]`, zoom `5.68`**. Kyoto projects to
canvas `(720, 431)` on a 1440×844 map — on the disc. Card stayed
**340×280**. The after screenshot is Japan, not the Africa globe.

Your reply matches. The suite line is
`{"zoom":5.68,"center":[135.75,35.12],"onDisc":true}`.

Kyoto's tile still reads Ishikawa Prefecture. That is parked. Do not
fix it.

---

## This round — the same fly at 390px

The phone checks open a register row and mark. They do not search Kyoto
on the card and tap Show on the map. At 390 the card is a **128px bar**
and the detail is a sheet. That is where this control will clip, hide,
or fly behind chrome.

**Done means:**

- Viewport **390×844**. Filters already on the map. Search Kyoto on the
  bar. Open the hit. **Show Kyoto on the map** is visible without
  `force`, at least **44×44**, not the accent.
- Tapping it moves the camera so Kyoto is on the disc. Selecting the
  hit still does not.
- The resting bar stays **≤128**. The kinds popover rule still holds:
  do not bury the globe.
- A check that fails if, at 390 with the register hidden, that control
  is missing, smaller than 44px, or tapping it leaves the camera where
  it was. Quote doc 3 §6.1 and §12.

I will load 390, search Kyoto, look for the control, tap it, and watch
the globe.

---

## Parked

Accounts, sync, `/api`, street/satellite/photoreal. Bevel. The 35 ids.
Kyoto → Ishikawa. `punchHoles`. Do not build them.

---

## Questions

Answer in `.cursor/AGENT-REPLY.md`:

1. Quote the last line of acceptance after a preview started *after*
   this build.
2. At 390, the control's width × height and `checkVisibility()`.
3. After the tap, center, zoom, and whether Kyoto is on the disc.

Standing rules: `.cursor/rules/travelers-world-map.mdc` outranks this file,
and `docs/2` outranks both.
