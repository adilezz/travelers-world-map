# Round 13 — 85/85 adds a stop in the suite; search on the card does not

Reviewed 2026-08-23 06:51 UTC on `:4173` (Round 12 preview, 05:39).
Hid the register, opened Trips, typed Kyoto into the card search, dumped
every painted node inside `.filters.on-map`, and ran

```
node test/acceptance.mjs http://127.0.0.1:4173/
```

Last line: **`85/85 passed`**. Exit 0. That matches your reply.

**How to talk back:** write in `.cursor/AGENT-REPLY.md`. Do not edit this file.

This round is **twelve minutes**. Do not start a backend.

---

## What the suite did

Hidden-register trip checks PASS: add-from-detail, no camera, LineString
of 4, card stays 340×280. `search on the full-screen card lists places
to open` reports **320×44 vis=true**.

---

## What I clicked — no hit

After Hide the register → Trips → type Kyoto, the painted card was:

coverage, All / Not visited / Visited, Search places, Kinds of place,
Passport, World Heritage.

**There was no Kyoto row.** `.row[data-id]` that `checkVisibility()`
accepted: **none**. `getByText('Japan')` resolved to a hidden
`<option value="JPN">` in the passport select. The first
`.row[data-id="JAP-c1857910"]` is the **register column**, not visible.

So the 320×44 the suite is proud of is not a node a traveler can tap on
that card. Same class of miss as the clipped kinds popover.

**Done means:**

- Hide the register. Type a place name in the card search. A hit appears
  **next to that search**, 44×44, `checkVisibility() === true`, not a
  register row in the closed column, not a `<option>`.
- I tap it without `force`. Detail opens on the map. **Add to …** puts
  it on the open trip. Camera does not move. LineString if two stops.
- Point the search-hit check at **that** node. If it is hidden, fail.
  Do not weaken 340×280.

I will screenshot the card with Kyoto typed and a hit I can see.

---

## Parked

Accounts, sync, `/api`, street/satellite/photoreal. Bevel. The 35 ids.
Kyoto → Ishikawa. `punchHoles`. Do not build them.

---

## Questions

Answer in `.cursor/AGENT-REPLY.md`:

1. Quote the last line of acceptance after a preview started *after*
   this build.
2. Which selector is 320×44? Quote it. If it is `.row` in the register,
   that is the bug.
3. Confirm I can tap a hit inside or directly under `.filters.on-map`
   without `force`.

Standing rules: `.cursor/rules/travelers-world-map.mdc` outranks this file,
and `docs/2` outranks both.
