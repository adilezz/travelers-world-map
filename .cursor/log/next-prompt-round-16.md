# Round 16 — the line is on the globe; I cannot fly to the stop

Reviewed 2026-08-23 07:42 UTC on `:4173` (Round 15 preview, 06:29). Hid the
register, opened Trips, added Kyoto then Tadrart Acacus from `.search-hit`,
and ran

```
node test/acceptance.mjs http://127.0.0.1:4173/
```

Last line: **`90/90 passed`**. Exit 0.

**How to talk back:** write in `.cursor/AGENT-REPLY.md`. Do not edit this file.

This round is **twelve minutes**. Do not start a backend.

---

## What I clicked — Round 15 is closed

`getSource('trip').getData()` (not the style JSON, not `_twmTrip`) returned
a LineString of **49** vertices from Kyoto `(135.75, 35.02)` to Acacus
`(10.33, 24.83)`. Layer `trip-line` reported **`rendered: 2`**, colour
**`#10262C`** — not the accent. Camera stayed. Card **340×280**. Day 1
holds both names. Unassigned is still a tray. The hop is **11224 km · a
long hop**.

The screenshot of that state is the Africa/Europe disc with a **dark
stroke** across North Africa / the Middle East that leaves at the eastern
limb. That is the trip. Your reply matches: Kyoto is behind the globe;
the visible run is Acacus → east.

---

## This round — Show the stop on the map

I hid the register, searched Kyoto, and opened it. The sheet came up. The
camera stayed on Africa. The line tells me the hop exists; it does not
put Kyoto in front of me.

Selecting must not zoom. Marking must not move the camera. The only
control wired to a camera move is an explicit **Show X on the map**.
Country and tile sheets already have that. The place sheet I opened from
the full-screen card did not give me one I could tap.

**Done means:**

- Hide the register. Search Kyoto. Open the hit. A **Show … on the map**
  control is on the on-map detail sheet, at least **44×44**, and does not
  use the accent.
- Tapping it moves the camera so Kyoto is on the disc. Selecting the hit
  still does not. Adding to a trip still does not.
- A check that fails if, with the register hidden, the open place sheet
  has no such control, or tapping it leaves the camera where it was.
  Quote doc 3 §6.1.
- 340×280 holds. The trip line stays a quiet straight segment.

I will search Kyoto from the card, look for the control, tap it, and
watch the globe.

---

## Parked

Accounts, sync, `/api`, street/satellite/photoreal. Bevel. The 35 ids.
Kyoto → Ishikawa. `punchHoles`. Do not build them.

---

## Questions

Answer in `.cursor/AGENT-REPLY.md`:

1. Quote the last line of acceptance after a preview started *after*
   this build.
2. What is the exact label of the control on the place sheet?
3. After the tap, what are center and zoom, and is Kyoto on the disc?

Standing rules: `.cursor/rules/travelers-world-map.mdc` outranks this file,
and `docs/2` outranks both.
