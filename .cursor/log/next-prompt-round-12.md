# Round 12 — dark and Trips park correctly; put a stop on the line

Reviewed 2026-08-23 06:30 UTC on `:4173` (Round 11 preview after trips
class fix, 05:23). Clicked moon + Trips with the register hidden, and ran

```
node test/acceptance.mjs http://127.0.0.1:4173/
```

Last line: **`79/79 passed`**. Exit 0.

**How to talk back:** write in `.cursor/AGENT-REPLY.md`. Do not edit this file.

This round is **twelve minutes**. Do not start a backend.

---

## What I clicked — Round 11 is closed

- **Dark, 1440, register hidden:** `data-theme=dark`, body
  `rgb(11, 22, 24)`, accent `#DBA83E` on **no** filter/header/Atlas
  chrome. Card still 340×280. Still unseen is on the card.
- **Trips on the map:** `.trips-on-map` at bottom-right **320×168**
  (1108, 720). Copy is right: *Straight lines, not routes — we do not
  invent travel times.* No times in the panel. Filter card stayed
  340×280. Globe still 1440×844.
- **390 with Trips open:** filter bar tucks to **48px**, trips **374×121**,
  map **390×434**. Still unseen remains in the register sheet.

No accent on that chrome. You opened Trips. Good.

---

## This round — the empty panel is not the feature

I saw New trip and the explanation. I did not see a place on a day, or
a line on the globe, **with the register hidden**.

**Done means:**

- Hide the register. Open Trips. Select a place (search on the card is
  enough). Add it to the trip. Assign a day without a time or duration.
- The map draws **straight segments**, not a route. Camera does not move
  from the add. Accent still unused on trip chrome.
- Card stays in the 340×280 band; trips stay `trips-on-map`. At 390 the
  globe is not buried.
- The existing trip checks (no camera, no duration, LineString) must
  still pass **in this layout**, not only with the column open. If they
  only exercise the column, add one that hides the register first.
  Quote doc 2 §9 / doc 3 §6.1. Do not weaken 340×280 or 79's dark checks.

I will Hide the register, add two places, and look for a LineString.

---

## Parked

Accounts, sync, `/api`, street/satellite/photoreal. Bevel. The 35 ids.
Kyoto → Ishikawa. `punchHoles`. Do not build them.

---

## Questions

Answer in `.cursor/AGENT-REPLY.md`:

1. Quote the last line of acceptance after a preview started *after*
   this build.
2. With the register hidden, how does a traveler add a place to the open
   trip — from the detail sheet, or only drag?
3. Confirm adding a stop does not move the camera in that layout.

Standing rules: `.cursor/rules/travelers-world-map.mdc` outranks this file,
and `docs/2` outranks both.
